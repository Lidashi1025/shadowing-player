# 双击影片画面切换全屏设计

左键双击影片画面时切换全屏／窗口。双击不得同时触发单击播放／暂停；因此影片元件
使用 Qt 系统双击间隔延迟确认单击。右键、拖动及未在画面内完成的手势不触发。

`ClickableVideoWidget` 新增 `double_clicked` 信号，并以单次计时器区分单击与双击。
`MainWindow` 将该信号连接到既有 `_toggle_fullscreen()`，不建立第二套窗口状态逻辑。

测试覆盖单击、双击互斥，以及主视窗连续双击在全屏与窗口之间切换；最后执行完整
测试、Windows 封装与冻结 EXE smoke test。
